en:
  activerecord:
    errors:
      models:
        collaborator:
          attributes:
            department_ids:
              not_exists: "can't be blank"
        department:
          attributes:
            collaborator_ids:
              exists: "can't delete when collaborators exists"
